package global

const (
	ORDER_CREATE = iota + 1
	ORDER_FINISH
	ORDER_CHECK
	ORDER_REFUND
	ORDER_AUTOCANCEL
	ORDER_OVERDUE
)

const (
	TICKET_CREATE = iota + 1
	TICKET_FINISH
	TICKET_CHECK
	TICKET_REFUND
	TICKET_AUTOCANCEL
	TICKET_OVERDUE
)
